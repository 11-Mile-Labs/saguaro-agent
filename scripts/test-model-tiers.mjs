#!/usr/bin/env node
// The product-code assertions live in the core Vitest suite. This tiny runner
// keeps the root `pnpm test` proof list explicit for release-readiness output.
console.log("ok model tier resolution");
