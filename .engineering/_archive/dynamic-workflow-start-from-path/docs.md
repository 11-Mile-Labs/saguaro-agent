# Docs

Public docs updated in `README.md` and `docs/workflow-run-resume.md`, plus generated marketplace copies from build.

Docs explain `workflow_path` behavior:

- concrete unlisted YAML files
- relative/absolute path handling
- YAML `name` match requirement
- run-state snapshot in `_workflow.json`
- `workflow_source` metadata
- neutral handling of caller-provided wording

## Knowledge Captures

- Stored project memory `mem_5d8ce3b3-e58a-4c7a-b5a2-281ea4344cd7` covering path-sourced resume order and `_workflow.json` source-of-truth behavior.
